use strict;
use warnings;

print "Write your number:\n";
my $a = <STDIN>;
print "Answer:\n";
print fact($a);
print "\n";
sub fact {
	my $N = shift;
	if($N <= 1){
		
		return 1;
	}
	else{ 
		return $N*fact($N-1);
	}
}
